export interface Product {
  name: string;
  slug: string;
  family: string;
  notes: string;
  image: string;
}

export interface Review {
  quote: string;
  initial: string;
  name: string;
  place: string;
}
